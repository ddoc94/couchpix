# Dependencies
node_modules/

# Build outputs
dist/
.wrangler/

# Deploy / CLI local state (linked to live projects, not source)
.vercel/

# Secrets — never commit. Worker secrets live in Cloudflare.
.dev.vars
.env
.env.*
*.local

# Claude Code tooling
.claude/

# Internal docs — kept on-disk (CLAUDE.md still auto-loads for Claude), out of
# the public repo. Anchored to the root so nested READMEs are unaffected.
/CLAUDE.md
/ROADMAP.md

# iOS / Capacitor generated artifacts (regenerate via pod install / cap sync)
apps/mobile/ios/App/Pods/
apps/mobile/ios/App/build/
apps/mobile/ios/App/App/public/
DerivedData/
*.xcuserstate
xcuserdata/

# OS
.DS_Store
